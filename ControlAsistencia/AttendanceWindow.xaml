<Window x:Class="ControlAsistencia.AttendanceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Marcaje" Height="380" Width="540" WindowStartupLocation="CenterScreen">
    <!-- Fondo con degradado suave -->
    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F4F9F9" Offset="0"/>
            <GradientStop Color="#E9FFFE" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>

    <!-- Recursos de estilo -->
    <Window.Resources>
        <!-- Tarjetas redondeadas para secciones -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="Background" Value="#E3FDFD"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <!-- Botón redondeado con sombra usando ControlTemplate -->
        <Style x:Key="RoundedPrimaryButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#30E3CA"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="24,12"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Width" Value="220"/>
            <Setter Property="Height" Value="120"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="root"
                                Background="{TemplateBinding Background}"
                                SnapsToDevicePixels="True"
                                CornerRadius="16">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="2" BlurRadius="12" Opacity="0.3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="root" Property="Opacity" Value="0.95"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="root" Property="Background" Value="#2CD8BE"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="root" Property="Background" Value="#BBDDD7"/>
                                <Setter TargetName="root" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Encabezado -->
        <Border Grid.Row="0" Style="{StaticResource Card}" Background="#3FC1C9" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="👤 Usuario: "
                           FontWeight="Bold"
                           FontSize="16"
                           Foreground="White"/>
                <TextBlock x:Name="TxtUsuario"
                           FontSize="16"
                           Foreground="White"/>
            </StackPanel>
        </Border>

        <!-- Estado actual -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Background="#E3FDFD" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="📌 Estado de hoy: "
                           FontWeight="Bold"
                           FontSize="14"
                           Foreground="#222"/>
                <TextBlock x:Name="TxtEstado" FontSize="14" Foreground="#444"/>
                <TextBlock Text="   |   " Foreground="#888"/>
                <TextBlock x:Name="TxtHora" FontSize="14" Foreground="#444"/>
            </StackPanel>
        </Border>

        <!-- Botón de marcaje (misma lógica y nombres) -->
        <Button Grid.Row="2"
                x:Name="BtnMarcar"
                Click="BtnMarcar_Click"
                Content="✅ Registrar"
                Style="{StaticResource RoundedPrimaryButton}"/>

        <!-- Mantengo tu botón "fantasma" pero oculto para no afectar lógica -->
        <Button Content="Button"
                Grid.Row="1"
                HorizontalAlignment="Left"
                Width="5"
                Height="0"
                Margin="-126,0,0,0"
                Visibility="Collapsed"/>
    </Grid>
</Window>
