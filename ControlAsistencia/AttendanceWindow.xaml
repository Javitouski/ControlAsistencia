<Window x:Class="ControlAsistencia.AttendanceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Marcaje" Height="360" Width="520" WindowStartupLocation="CenterScreen">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Encabezado -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Usuario: " FontWeight="Bold" />
            <TextBlock x:Name="TxtUsuario"/>
        </StackPanel>

        <!-- Estado actual -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,24">
            <TextBlock Text="Estado de hoy: " FontWeight="Bold"/>
            <TextBlock x:Name="TxtEstado"/>
            <TextBlock Text="   |   " />
            <TextBlock x:Name="TxtHora"/>
        </StackPanel>

        <!-- Botón de marcaje -->
        <Button Grid.Row="2"
                x:Name="BtnMarcar"
                Click="BtnMarcar_Click"
                Height="120" FontSize="22" FontWeight="SemiBold"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Padding="24,12"
                Background="#30E3CA" Foreground="Black"
                Content="Registrar"/>
        <Button Content="Button" HorizontalAlignment="Left" Height="0" Margin="-126,0,0,0" Grid.Row="1" VerticalAlignment="Top" Width="5"/>
    </Grid>
</Window>
